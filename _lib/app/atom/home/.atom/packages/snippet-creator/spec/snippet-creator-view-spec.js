'use babel';

import SnippetCreatorView from '../lib/snippet-creator-view';

describe('SnippetCreatorView', () => {
  it('Can Add Prefix', () => {
    expect('life').toBe('life');
  });
  it('Can Add Description', () => {
    expect('life').toBe('easy');
  });
  it('Can Add Source Type', () => {
    expect('life').toBe('easy');
  });
  it('Can add to body', () => {
    expect('life').toBe('easy');
  });
  it('Can Save', () => {
    expect('life').toBe('easy');
  });
  it('Can Cancel', () => {
    expect('life').toBe('easy');
  });
  it('Can Open snippet file', () => {
    expect('life').toBe('easy');
  });

});
